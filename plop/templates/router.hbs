import { Router, Request, Response } from "express";

export var router_{{name}}: Router = Router()

router_{{name}}.get('/',  (req: Request, res:Response) => {
    res.send('Welcome to the /{{name}} route');
})